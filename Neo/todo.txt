TODO:

renderPass --- swapChain render pass.
- subpasses...?? clear, etc.
- viewport
- clip rectangle


material - renderPass list. - creates pipeline per renderpass,  apply material checks for renderpass


render scene file:

  list of render passes to use
  client can addTask for each renderpass.

renderPass (x)
- add task (renderpass).

renderPass (y)
- add task (renderpass).



